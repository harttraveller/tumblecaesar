from setuptools import setup, find_packages

setup(
    name="tumblecaesar",
    version="0.0",
    description="A module loosely based on the Caesar cipher, which enables the encoding/obfuscation of strings.",
    author="Hart Traveller",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
